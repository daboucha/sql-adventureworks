-- List the customers along with their ID numbers. Include the last names, first names, and company names
SELECT CustomerID, FirstName, LastName, CompanyName
FROM SalesLT.Customer;

-- list the name, product number, and color of each product
SELECT Name, ProductNumber, Color
FROM SalesLT.Product;